# 安全说明

## 支持的版本

仅最新版本接受安全相关的修复。建议始终使用最新 Release。

## 请勿在公开渠道泄露

- **不要在 Issue 或任何公开渠道上传 `.keystore` / `.jks` / `.p12` 签名文件**。
- **不要公开密码、Token、API Key**：包括签名密码、任意服务凭据。
- **不要提交本地配置文件**：`.env`、`*.local`、`local.properties`、`*.csproj.user`、`signing.local.properties` 等。
- **不要提交构建产物与个人资料**：APK、EXE、ZIP、`.idsig`、keystore、缓存、`上传资料/` 目录。

以上内容均已由项目根目录的 `.gitignore` 忽略；提交前请再次确认暂存区内容。

## 报告安全问题

如果你发现了安全问题（而非一般 bug），请**不要**在公开 Issue 中描述细节。

请私下报告：

- 通过 GitHub 的 **Security Advisory**（如果仓库已启用），或
- 通过项目所有者提供的私有联系方式。

报告中请包含：

- 问题描述与影响。
- 复现步骤。
- 建议的修复方向。

我们会在确认后尽快修复并发布更新。

## 签名证书泄露的处理

Android 正式签名证书（keystore）泄露后：

1. **立即更换 keystore 与所有相关密码**。
2. **升级应用签名**：更换证书后，已安装用户无法通过覆盖升级，必须引导用户卸载重装或通过系统升级通道更新。
3. **在更新日志中明确说明签名变更**，避免用户因「签名不一致」无法升级而困惑。
4. 如果 keystore 曾提交到 Git 历史，视为已泄露，按上述流程处理；即使已删除文件，历史中的内容仍可能被访问。

## 隐私承诺

- 收藏、缓存全部保存在本机，不收集用户账号信息。
- 不上传用户收藏到任何作者服务器。
- 应用仅访问 Open Library 公开接口与本地文件。
